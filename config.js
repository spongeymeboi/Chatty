// Leave empty for normal same-server hosting.
// For a separate static frontend (GitHub Pages/jsDelivr), set:
// window.CHAT_CONFIG = { WS_URL: "wss://your-chat-server.example" };

window.CHAT_CONFIG = window.CHAT_CONFIG || {
  WS_URL: ""
};
